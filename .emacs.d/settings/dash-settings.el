;-----------------------------------------------------------;
;;;  An Excellent Modern APIs for Emacs: dash, f, and s   ;;;
;-----------------------------------------------------------;

(require 'dash)
(require 'f)
(require 's)

(eval-after-load "dash" '(dash-enable-font-lock))

(provide 'dash-settings)
